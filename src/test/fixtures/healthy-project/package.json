{
  "name": "healthy-project",
  "version": "1.0.0",
  "description": "A healthy test project",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src",
    "format": "prettier --write src"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
