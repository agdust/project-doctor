# project-doctor

A tool that helps fix common issues in projects. Consider it project-wide technology-independent linter.

## Usage

To open interactive CLI wizard, just laucn this package

```sh
npx project-doctor
```

```sh
# if we create `doctor` script in package.json
npm run doctor
```
